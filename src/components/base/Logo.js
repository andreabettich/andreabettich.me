import React from 'react';
import Icon from '../basic/Icon';

const Logo = props => {
  return (
    <div className="logo">
      <Icon icon="logo" height="3rem" width="50px" />
    </div>
  );
};

export default Logo;
